/* The main code for this game. This right here will handle
   the majority of updating and rendering the game onto the
   screen. OpenGL stuff will obviously be abstracted a bit.
*/

namespace jtg {

    /* Runs the game */
    void Start_Game();
}